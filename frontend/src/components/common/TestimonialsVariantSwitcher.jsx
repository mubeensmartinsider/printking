/* RETIRED — the temporary testimonials layout comparison switcher
   (Grid / Marquee / Featured / Band / Columns / Mosaic / Bars / Wall).

   Columns was selected and is now the only implementation, living in
   components/home/Testimonials.jsx. Nothing imports this file.
   It is safe to delete. */
export default null;
