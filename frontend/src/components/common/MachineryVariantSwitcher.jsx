/* ============================================================
   RETIRED — the Grid / Rail / Spotlight comparison switcher that
   picked the final machinery layout. Spotlight was chosen and the
   switcher is no longer used by any component.

   This file is intentionally inert. Delete it at your convenience.
   ============================================================ */

export default null;
