import React from "react";

/* ============================================================
   Custom cursor — disabled for performance.
   Native cursor is used instead to avoid continuous rAF loop.
   ============================================================ */
export default function Cursor() {
  return null;
}
